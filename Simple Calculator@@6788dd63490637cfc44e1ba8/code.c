#include <stdio.h>
int main() {
    int a,b;
    char op;
    scanf("%d" "%d",&a,&b);
    scanf("%c",&op);
    if (op=='+'){
        result=a+b
        printf("%d",result);
    }
    return 0;
}