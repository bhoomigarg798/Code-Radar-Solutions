#include <stdio.h>
void reversstribg(char arr[]) {
    for (int arr[i]!='\0';i>0;i--) {
        printf("%s",arr[i])
    }
}
int main() {
    char s[];
    scanf("%s",s);
    reversstribg(s[]);
    return 0;

}