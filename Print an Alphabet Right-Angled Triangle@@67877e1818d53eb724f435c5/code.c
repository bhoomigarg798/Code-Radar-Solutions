#include <stdio.h>
int main() {
    int N;
    scanf("%d",&N);
    for (int i=A;i<=N;i++) {
        for (j=A;j<=i;j++) {
            printf("%c",N);
        }
        printf("\n");
    }
    return 0;
}